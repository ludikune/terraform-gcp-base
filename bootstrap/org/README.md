# terraform-gcp-base / bootstrap / org

This piece of code will create the project. In order to delete it you must add this to the project first:

deletion_policy = "DELETE"

then you can use "terraform destroy".
